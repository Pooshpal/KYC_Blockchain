import { default as parseDuration } from "parse-duration";
import multibase = require("multibase");
export const command: string;
export const describe: string;
export const builder: {
    type: {
        type: string;
        alias: string;
        default: string;
        choices: string[];
        describe: string;
    };
    quiet: {
        type: string;
        alias: string;
        default: boolean;
        describe: string;
    };
    'cid-base': {
        describe: string;
        type: string;
        choices: string[];
    };
    timeout: {
        type: string;
        coerce: typeof parseDuration;
    };
};
/**
 * @param {object} argv
 * @param {import('../../types').Context} argv.ctx
 * @param {string[]} argv.ipfsPath
 * @param {'direct' | 'indirect' | 'recursive' | 'all'} argv.type
 * @param {boolean} argv.quiet
 * @param {import('multibase').BaseName} argv.cidBase
 * @param {number} argv.timeout
 */
export function handler({ ctx: { ipfs, print }, ipfsPath, type, quiet, cidBase, timeout }: {
    ctx: import("../../types").Context;
    ipfsPath: string[];
    type: "recursive" | "all" | "direct" | "indirect";
    quiet: boolean;
    cidBase: multibase.BaseName;
    timeout: number;
}): Promise<void>;
/**
 * @param {object} argv
 * @param {import('../../types').Context} argv.ctx
 * @param {string[]} argv.ipfsPath
 * @param {'direct' | 'indirect' | 'recursive' | 'all'} argv.type
 * @param {boolean} argv.quiet
 * @param {import('multibase').BaseName} argv.cidBase
 * @param {number} argv.timeout
 */
export function handler({ ctx: { ipfs, print }, ipfsPath, type, quiet, cidBase, timeout }: {
    ctx: import("../../types").Context;
    ipfsPath: string[];
    type: "recursive" | "all" | "direct" | "indirect";
    quiet: boolean;
    cidBase: multibase.BaseName;
    timeout: number;
}): Promise<void>;
//# sourceMappingURL=ls.d.ts.map