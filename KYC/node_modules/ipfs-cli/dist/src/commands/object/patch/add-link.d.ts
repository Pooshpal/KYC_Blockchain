import { default as parseDuration } from "parse-duration";
import multibase = require("multibase");
export const command: string;
export const describe: string;
export const builder: {
    root: {
        type: string;
        coerce: (value: any) => import("cids") | undefined;
    };
    ref: {
        type: string;
        coerce: (value: any) => import("cids") | undefined;
    };
    'cid-base': {
        describe: string;
        type: string;
        choices: string[];
    };
    'cid-version': {
        describe: string;
        type: string;
        default: number;
    };
    timeout: {
        type: string;
        coerce: typeof parseDuration;
    };
};
/**
 * @param {object} argv
 * @param {import('../../../types').Context} argv.ctx
 * @param {import('cids')} argv.root
 * @param {string} argv.name
 * @param {import('cids')} argv.ref
 * @param {import('multibase').BaseName} argv.cidBase
 * @param {import('cids').CIDVersion} argv.cidVersion
 * @param {number} argv.timeout
 */
export function handler({ ctx: { ipfs, print }, root, name, ref, cidBase, cidVersion, timeout }: {
    ctx: import("../../../types").Context;
    root: import("cids");
    name: string;
    ref: import("cids");
    cidBase: multibase.BaseName;
    cidVersion: import("cids").CIDVersion;
    timeout: number;
}): Promise<void>;
/**
 * @param {object} argv
 * @param {import('../../../types').Context} argv.ctx
 * @param {import('cids')} argv.root
 * @param {string} argv.name
 * @param {import('cids')} argv.ref
 * @param {import('multibase').BaseName} argv.cidBase
 * @param {import('cids').CIDVersion} argv.cidVersion
 * @param {number} argv.timeout
 */
export function handler({ ctx: { ipfs, print }, root, name, ref, cidBase, cidVersion, timeout }: {
    ctx: import("../../../types").Context;
    root: import("cids");
    name: string;
    ref: import("cids");
    cidBase: multibase.BaseName;
    cidVersion: import("cids").CIDVersion;
    timeout: number;
}): Promise<void>;
//# sourceMappingURL=add-link.d.ts.map