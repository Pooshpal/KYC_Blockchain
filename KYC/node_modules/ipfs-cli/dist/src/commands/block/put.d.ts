import { default as parseDuration } from "parse-duration";
import multibase = require("multibase");
export const command: string;
export const describe: string;
export const builder: {
    format: {
        alias: string;
        describe: string;
        default: string;
    };
    mhtype: {
        describe: string;
        default: string;
    };
    mhlen: {
        describe: string;
        default: undefined;
    };
    version: {
        describe: string;
        type: string;
        default: number;
    };
    'cid-base': {
        describe: string;
        type: string;
        choices: string[];
    };
    pin: {
        describe: string;
        type: string;
        default: boolean;
    };
    timeout: {
        type: string;
        coerce: typeof parseDuration;
    };
};
/**
 * @param {object} argv
 * @param {import('../../types').Context} argv.ctx
 * @param {string} argv.block
 * @param {import('multicodec').CodecName} argv.format
 * @param {import('multihashes').HashName} argv.mhtype
 * @param {number} argv.mhlen
 * @param {import('cids').CIDVersion} argv.version
 * @param {boolean} argv.pin
 * @param {import('multibase').BaseName} argv.cidBase
 * @param {number} argv.timeout
 */
export function handler({ ctx: { ipfs, print, getStdin }, block, timeout, format, mhtype, mhlen, version, cidBase, pin }: {
    ctx: import("../../types").Context;
    block: string;
    format: import("multicodec").CodecName;
    mhtype: import("multihashes").HashName;
    mhlen: number;
    version: import("cids").CIDVersion;
    pin: boolean;
    cidBase: multibase.BaseName;
    timeout: number;
}): Promise<void>;
/**
 * @param {object} argv
 * @param {import('../../types').Context} argv.ctx
 * @param {string} argv.block
 * @param {import('multicodec').CodecName} argv.format
 * @param {import('multihashes').HashName} argv.mhtype
 * @param {number} argv.mhlen
 * @param {import('cids').CIDVersion} argv.version
 * @param {boolean} argv.pin
 * @param {import('multibase').BaseName} argv.cidBase
 * @param {number} argv.timeout
 */
export function handler({ ctx: { ipfs, print, getStdin }, block, timeout, format, mhtype, mhlen, version, cidBase, pin }: {
    ctx: import("../../types").Context;
    block: string;
    format: import("multicodec").CodecName;
    mhtype: import("multihashes").HashName;
    mhlen: number;
    version: import("cids").CIDVersion;
    pin: boolean;
    cidBase: multibase.BaseName;
    timeout: number;
}): Promise<void>;
//# sourceMappingURL=put.d.ts.map